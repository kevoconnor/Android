<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle">@style/NormalButton</item>
        <item name="android:editTextColor">@color/mediumBlue</item>
        <item name="android:textSize">30sp</item>
    </style>

    <style name="StylePlox" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/mediumBlue</item>
        <item name="android:textColor">@color/whiteish</item>
    </style>

    <style name="HeaderText" parent="android:TextAppearance.Medium">
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/darkBlue</item>
    </style>

    <style name="NormalText" parent="android:TextAppearance.Large">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/lightBlue</item>
    </style>

    <style name="NormalButton" parent="android:Widget.Holo.Button">
        <item name="android:background">@color/whiteish</item>
        <item name="android:textColor">@color/darkBlue</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="NewEditText" parent="android:TextAppearance.Small">
        <item name="android:textSize">50sp</item>
    </style>

</resources>
